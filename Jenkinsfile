String tenancy_ocid = "Test"

pipeline {
	
    agent any 
    stages {
        stage('Stage Init Atp Variables') {
            steps {
                echo "${tenancy_ocid}"
            }
        }
    }
}
